# APEX-9 — Python engine dependencies
# Install:  pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
statsmodels>=0.14

# MetaTrader5 is WINDOWS-ONLY and will NOT install on macOS.
# It is imported lazily (try/except) — the Mac runs in signal-file mode,
# where Python writes apex9_signals.json and the MQL5 EA executes trades.
# Only uncomment if you run the Python engine on Windows:
# MetaTrader5>=5.0.45
